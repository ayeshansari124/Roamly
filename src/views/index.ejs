<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Roamly — Curated Travel Experiences</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>

<body class="relative min-h-screen bg-black text-white">

  <!-- Background -->
  <div class="absolute inset-0 -z-10">
    <img 
      src="https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&w=1500&q=80"
      alt="travel background"
      class="w-full h-full object-cover brightness-50"
    />
  </div>

  <!-- NAVBAR -->
  <nav class="flex items-center justify-between px-6 py-6 max-w-7xl mx-auto relative z-10">
    <h1 class="text-2xl sm:text-3xl font-extrabold tracking-wide flex items-center gap-2">
      🌍 Roamly
    </h1>

    <div class="flex gap-3">
      <button
        onclick="openModal('loginModal')"
        class="px-4 py-2 bg-white/10 hover:bg-white/20 rounded-lg font-semibold transition text-sm border border-white/20">
        Login
      </button>

      <button
        onclick="openModal('registerModal')"
        class="px-4 py-2 bg-yellow-400 text-black hover:bg-yellow-500 rounded-lg font-semibold transition text-sm">
        Sign Up
      </button>
    </div>
  </nav>

  <!-- HERO -->
  <section class="h-[85vh] flex flex-col items-center justify-center text-center px-6 relative z-10">
    <h2 class="text-4xl sm:text-6xl font-black mb-6 leading-tight">
      Discover Experiences,<br class="hidden sm:block"/> Not Just Places
    </h2>

    <p class="max-w-2xl text-lg sm:text-xl opacity-90">
      Hand-picked adventures, small-group trips, and local experiences —
      curated for travelers who want more than sightseeing.
    </p>

    <div class="flex gap-4 mt-10 flex-wrap justify-center">
      <a
        href="/explore"
        class="px-7 py-3 bg-yellow-400 text-black rounded-xl font-bold text-lg hover:bg-yellow-500 transition">
        Explore Experiences
      </a>

      <button
        onclick="openModal('registerModal')"
        class="px-7 py-3 bg-white/10 rounded-xl font-bold text-lg hover:bg-white/20 transition border border-white/20">
        Plan My Trip
      </button>
    </div>
  </section>

  <!-- LOGIN MODAL -->
  <div id="loginModal" class="hidden fixed inset-0 bg-black/70 z-50 flex items-center justify-center p-4">
    <div class="bg-zinc-900 p-8 rounded-xl max-w-md w-full shadow-2xl">
      <h2 class="text-2xl font-bold mb-6 text-center">Welcome Back</h2>

      <form action="/login" method="POST" class="flex flex-col gap-4">
        <input type="email" name="email" required placeholder="Email"
          class="p-3 bg-zinc-800 rounded-md focus:ring-2 focus:ring-yellow-400 outline-none">

        <input type="password" name="password" required placeholder="Password"
          class="p-3 bg-zinc-800 rounded-md focus:ring-2 focus:ring-yellow-400 outline-none">

        <button class="py-3 bg-yellow-400 text-black rounded-lg font-bold hover:bg-yellow-500">
          Login
        </button>
      </form>

      <button onclick="closeModal('loginModal')" class="mt-4 w-full text-zinc-400 hover:text-white text-sm">
        Cancel
      </button>
    </div>
  </div>

  <!-- REGISTER MODAL -->
  <div id="registerModal" class="hidden fixed inset-0 bg-black/70 z-50 flex items-center justify-center p-4">
    <div class="bg-zinc-900 p-8 rounded-xl max-w-md w-full shadow-2xl">
      <h2 class="text-2xl font-bold mb-6 text-center">Start Your Journey</h2>

      <form action="/register" method="POST" class="flex flex-col gap-4">
        <input type="text" name="name" required placeholder="Full Name"
          class="p-3 bg-zinc-800 rounded-md focus:ring-2 focus:ring-yellow-400 outline-none">

        <input type="email" name="email" required placeholder="Email"
          class="p-3 bg-zinc-800 rounded-md focus:ring-2 focus:ring-yellow-400 outline-none">

        <input type="password" name="password" required placeholder="Password"
          class="p-3 bg-zinc-800 rounded-md focus:ring-2 focus:ring-yellow-400 outline-none">

        <button class="py-3 bg-yellow-400 text-black rounded-lg font-bold hover:bg-yellow-500">
          Create Account
        </button>
      </form>

      <button onclick="closeModal('registerModal')" class="mt-4 w-full text-zinc-400 hover:text-white text-sm">
        Cancel
      </button>
    </div>
  </div>

  <script>
    function openModal(id) {
      document.getElementById(id).classList.remove("hidden");
    }
    function closeModal(id) {
      document.getElementById(id).classList.add("hidden");
    }
  </script>

</body>
</html>