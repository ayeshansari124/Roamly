<nav class="bg-white border-b sticky top-0 z-40">
  <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">

    <h1 class="text-2xl font-extrabold">🌍 Roamly</h1>

    <div class="flex items-center gap-4 text-sm font-semibold">

      <a href="/explore" class="text-gray-600 hover:text-black">
        Explore
      </a>

      <% if (isAuthenticated) { %>
        <a href="/my-bookings" class="text-gray-600 hover:text-black">
          My Bookings
        </a>

        <% if (userRole === "admin") { %>
          <a href="/admin/dashboard" class="text-gray-600 hover:text-black">
            Admin
          </a>
        <% } %>

        <form action="/logout" method="POST">
          <button class="text-red-500 hover:text-red-700">
            Logout
          </button>
        </form>
      <% } else { %>
        <a href="/" class="text-gray-600 hover:text-black">Login</a>
        <a href="/" class="text-yellow-500 hover:text-yellow-600">Sign Up</a>
      <% } %>

    </div>

  </div>
</nav>
